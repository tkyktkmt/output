役割）
  Git内のあらゆるコマンド操作の全ての履歴を参照する
  git reset の変更を取り消したい場合などに用いる
  # git log はcommitの履歴のみを参照する

文法）
  git reflog
  レスポンス ①コミット番号 ②(参照名) ③HEAD@{n}: ④Gitコマンド: ⑤処理内容
  ①コミット番号
    各Git操作の内容をハッシュ値をつけて参照できるようにしたもの
    本来は桁数が非常に多いが、7桁ほどに省略された値が表示される
    ハッシュ値が7桁だと重複が発生する場合は、8桁、9桁のように重複の発生しない桁数だけ表示される
  ②（参照名）
    参照名は (HEAD -> main)のようにカッコ()で囲まれている部分をさす
    ローカルブランチとリモート追跡ブランチが各々どこにあるかを示す
    「HEAD->」：現在のコミット履歴を参照しているブランチの場所を表す  例）「HEAD -> main」は、現在のブランチが「main」にいることを示す
  ③HEAD@{n} # HEAD@{n} = @@{n} Gitでは、HEADのエイリアスとして「@」が用意されている
    全てのGitコマンドの操作履歴において、最新の履歴を「0」として、そこから何番目の履歴かを表す
  ④Gitコマンド
    実行されたGitコマンドの情報
  ⑤処理内容
    処理内容